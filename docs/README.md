# docs/

Reference material and design notes related to Lucindri.

## Contents

- **[indri-query-language.md](indri-query-language.md)** — a guide to the full Indri structured
  query language, annotated with what Lucindri actually supports (✅/⚠️/❌). Authored from
  knowledge of the language and a reading of Lucindri's parser; the authoritative upstream
  source is the Lemur Project wiki (linked inside the file).

- **[query-biased-summaries.md](query-biased-summaries.md)** — how to produce query-biased
  extractive summaries of a result document "the Lucene way" (`UnifiedHighlighter` with a sentence
  `BreakIterator`), since Lucindri lacks Indri's passage/extent operators; plus LM-faithful
  alternatives (ephemeral per-doc index; sentence-granular index).
- **[lucindri-vs-indri.md](lucindri-vs-indri.md)** — Lucindri↔C++ Indri query-language conformance
  catalog (generated by `../scripts/diff_indri.sh`).
- **[lucindri-vs-indri-scores.md](lucindri-vs-indri-scores.md)** — score-level conformance on integer
  collections (TASK-0010): per-operator match/diverge vs Indri and vs the analytic Dirichlet formula,
  with tokenizer/stemmer/stopwords eliminated. Reproduce with `../scripts/trec-comparison/score_probe.sh`.
- **[trec-comparison.md](trec-comparison.md)** — large-scale Lucindri↔Indri comparison on a full TREC
  collection (disks 4&5, topics 401–450): overlap@k/RBO agreement, `#weight` parity (confirmed), and
  the tokenizer difference that drives the residual divergence. Reproduce with `../scripts/compare_trec.sh`.

## Related documents kept elsewhere

- The maintained project overview is the repo-root `README.md`.
- Working notes for Claude sessions are in the repo-root `CLAUDE.md`.
- Tasks live in `../tasks/` (`TASK-NNNN.md`).
- External design docs referenced during this work (Cottontail / GCL, by C.L.A. Clarke) live in
  the separate Cottontail repo under `docs/`, e.g. `Annotative-Indexing-IRRJ-Clarke-2025.md`
  (defines GCL) and `phrase-search-performance-and-proposal.md` (phrase-in-cover performance).
  They are not copied here to avoid duplicating another project's docs.
