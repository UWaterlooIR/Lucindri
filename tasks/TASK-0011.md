# TASK-0011 — Large-scale differential fuzzing (Lucindri vs C++ Indri) on a realistic integer collection

- **Status:** Done (2026-07-05)
- **Created:** 2026-07-04
- **Owner:** (unassigned)
- **Summary:** Build a *realistic* integer collection by replacing each analyzed word in a real
  corpus (LATimes, or a subset of `t45minusCR`) with a unique integer id — preserving term counts and
  positions — so tokenization/stemming/stopwords are neutralized but the term distribution matches
  real English. Then **fuzz a large variety of legal Indri queries** and diff per-document scores
  between Lucindri and C++ Indri to surface any remaining scoring/query-semantics divergences.

## Why

TASK-0010 verified operators on tiny toy collections (C1/C2). This scales it up: a realistic
distribution (Zipfian term frequencies, real doc lengths, real co-occurrence and proximity patterns)
exercises the scorers the way real queries do, while **integers keep the input identical across
engines** (the tokenizer/stemmer/stopword residuals from TASK-0008 are removed by construction, so
*any* divergence is a real bug — not analysis). Random legal queries reach corners hand-written tests
miss. This is differential testing: Indri is the reference oracle, but per TASK-0010 we also sanity
-check surprising results against hand computation / internal consistency (Indri is not infallible).

## Build the integer collection (via Indri's tokenization + forward index)

Let Indri do the tokenization and read the tokens back from its **forward index** (document vectors),
keeping **all** tokens (no stopword removal).

1. Choose a source: **LATimes** (~130K docs) or a tractable subset of `t45minusCR` (e.g. 10k–50k docs).
2. Index it in Indri with **no stopper** (keep all tokens) and Krovetz stemming (or none — the integer
   output is stem-invariant either way): `<stemmer><name>krovetz</name></stemmer>`, no `<stopper>`.
3. Build a **global** `term → integer id` map from `dumpindex <idx> v` (the vocabulary; assign ids by
   descending `cf` so common words get small ids, Zipfian like a real index — id values are just
   labels). Note: `dumpindex dv`'s numeric id column is **document-local**, so map by the term
   **string** (3rd column), not that id.
4. For each document, `dumpindex <idx> dv <docid>` → the term strings in position order (all tokens,
   incl. what would be stopwords). Emit trectext with `<TEXT>` = those tokens' global integer ids:
   `<DOC><DOCNO>orig-docno</DOCNO><TEXT>id id id …</TEXT></DOC>`. Order = positions, repetition = tf.
   (dv-per-doc is O(numDocs) dumpindex calls — fine for a subset; use the Indri API for full scale.)
5. Index the integer corpus in **both** engines with analysis disabled so the integers pass through
   unchanged: Indri `word` tokenizer + no stemmer + no stopper; Lucindri whitespace/no-op tokenizer,
   `stemmer=none` (or kstem — a no-op on ints), `removeStopwords=false`. Integers have no punctuation,
   so both tokenize identically, and since all tokens are kept both agree on `|d|`/`|C|`.
6. **Verify Phase-0 alignment** (must be exact before comparing): `numDocs`, `|C|`, per-id `cf`/`df`,
   per-doc `|d|`, and a spot-checked id's positions match between engines. Because all tokens are kept
   (no stopword removal), document lengths are the full realistic lengths — this also exercises the
   distribution that TASK-0009 (stopword-in-`|d|`) concerns, though here both engines see identical
   integers so they agree.

## Fuzz legal Indri queries

Generate N random queries (seeded, reproducible) from a grammar that respects Indri's rules:

- **Leaves:** integer ids, biased toward frequent ids (so results are non-empty) with some rare/OOV.
- **Belief operators** (operands may be anything): `#combine`, `#weight`/`#wsum` (random positive
  weights), `#or`, `#not` (nested), `#max`, `#syn`, plus nesting to a random depth.
- **Proximity operators** (operands must be terms / proximity / `#syn` — NOT belief operators):
  `#1`/`#odN`, `#uwN` (random N), `#band`; include compound operands (`#syn(...)`, nested windows).
- **Filters:** `#filreq`/`#filrej` (Indri) ↔ `#scoreif`/`#scoreifnot` (Lucindri) — emit the correct
  name per engine; `(condition scored)` argument order.
- Enforce the nesting constraints so every generated query is legal in both engines (avoid the known
  "belief operand inside a proximity" error). Emit each query in **both dialects** (operator-name map).
- Sweep smoothing too: Dirichlet at a few μ, and Jelinek–Mercer at a few λ.

## Differential harness

- Run each query on both engines (`IndriRunQuery` vs `IndriSearch`, `trecFormat`, `count` ≥ a few k).
- Compare **per-document scores** (tolerance ~1e-3 on the shared docs) and the retrieved **sets**
  (overlap@k). Flag any query with a score delta above tolerance or a set mismatch.
- For each divergence: **auto-minimize** (shrink the query and/or restrict to a few docs until it
  still diverges), classify, and check the smaller case against hand computation. Then fix (with a
  regression test, per the testing policy) or catalog with the minimal repro.
- Report: number of queries run, % exact-match, and a ranked list of distinct divergence signatures.

## Deliverables

- `scripts/trec-comparison/build_integer_corpus.*` — real corpus → integer trectext + `term↔id` map.
- `scripts/trec-comparison/fuzz_queries.*` — seeded legal-Indri-query generator (both dialects).
- `scripts/trec-comparison/diff_fuzz.sh` — run both engines, diff scores/sets, minimize divergences.
- Findings appended to `docs/lucindri-vs-indri-scores.md`; any bug fixes ship with regression tests
  (reuse the count/score-level single-doc technique where possible).

## Acceptance criteria

- A realistic integer collection built and Phase-0-aligned between the engines (exact `|C|`/`cf`/`|d|`).
- ≥ a few thousand fuzzed legal queries run through both engines with a reported exact-match rate.
- Every distinct divergence signature is root-caused to a minimal repro and either fixed (with a
  regression test) or cataloged as a known difference (with rationale, e.g. an Indri quirk).
- Reproducible from a fixed seed.

## Implementation plan (ordered)

Reuse the TASK-0010 tooling: `scripts/trec-comparison/score_probe.sh` (build/run pattern),
`agreement.py` (overlap@k), the `DumpFieldStats`/`DumpPos`/`DumpTerms` Lucene probes, and the
engine paths (`IndriBuildIndex`, `dumpindex`, `IndriRunQuery`, `IndriSearch`). Work on a branch.

### A. Build the integer corpus  → `scripts/trec-comparison/build_integer_corpus.sh` (+ a Python helper)

1. **Choose source & scale.** Start with a subset for a first pass: the first `N` documents of
   LATimes (`t45minusCR/latimes.dat.gz`), e.g. `N = 20000`. Keep `N` a parameter.
2. **Index the source in Indri** with **no stopper** and Krovetz: build param with
   `<stemmer><name>krovetz</name></stemmer>` and no `<stopper>`. Record `numDocs` from `dumpindex s`.
3. **Global term→id map** from `dumpindex <idx> v`: parse `term cf df` lines, sort by `cf` desc,
   assign ids `1..V` (common words = small ids). Write `vocab.tsv` (`id \t term \t cf`). Also build the
   reverse `term→id` dict in the Python helper.
4. **Emit integer docs.** For `docid = 1..N`: `dumpindex <idx> dv <docid>` → take the term **strings**
   (3rd column, in position order — ALL tokens); optionally `dumpindex <idx> dn <docid>` for the real
   DOCNO (else use `d<docid>`). Map each string → its global id; write
   `<DOC><DOCNO>…</DOCNO><TEXT>id id …</TEXT></DOC>` to `integers.trec`. (Ignore `dv`'s id column — it
   is document-local, verified.) **Scale note:** the `dv` shell loop is one process per doc — fine for
   ≤ ~50k; for full LATimes write a tiny Indri-API program (`dump_forward`) that walks the direct index
   in one pass. Prototype on the subset first.
5. **Index the integer corpus in both engines** (analysis off): Indri `trectext` (word tokenizer), no
   stemmer, no stopper; Lucindri `documentFormat=trectext, contentTags=text, stemmer=kstem` (no-op on
   ints), `removeStopwords=false, ignoreCase=true`. Integer strings tokenize identically in both.
6. **Verify Phase-0 alignment (gate — must be exact before any scoring):** `numDocs`, `|C|`
   (`dumpindex s` vs `DumpFieldStats`), a sample of ids' `cf` (`dumpindex x <id>` vs a Lucene probe),
   and one doc's positions (`dumpindex dv` vs `DumpPos`). Abort and fix the builder on any mismatch.

### B. Query fuzzer  → `scripts/trec-comparison/fuzz_queries.py` (seeded, emits BOTH dialects)

7. **Grammar (recursive, depth ≤ 4, breadth 1–4), enforcing legality:**
   - *Belief context* (top level and belief operands): `TERM`, `#combine`, `#weight`/`#wsum` (random
     positive weights), `#or`, `#max`, `#not` (only nested, e.g. `#combine(a #not(b))`), `#syn`,
     proximity ops, filters.
   - *Proximity context* (operands of `#N`/`#uwN`/`#band`): only `TERM`, proximity op, or `#syn` —
     **never** a belief operator (avoids the known "belief operand in proximity" error).
   - *Filters* (top level or belief child): `(condition scored)` where condition is term/proximity and
     scored is a belief query. Render as `#filreq`/`#filrej` (Indri) vs `#scoreif`/`#scoreifnot`
     (Lucindri).
   - Proximity distances `N ∈ 1..15`; use `#N` (not `#odN`) so both dialects agree.
8. **Term selection:** bias toward frequent ids (small ids) so results are non-empty; sprinkle rare
   ids and a few **OOV** ids (id `> V`) to exercise the floor / OOV-in-proximity paths.
9. **Emit:** for a fixed `--seed`, generate `M` queries (start `M=2000`). Write an Indri run `.param`
   (Indri dialect) and a Lucindri `.xml` (Lucindri dialect, `<removeStopwords>false</removeStopwords>`),
   each with the same `<number>`s. Do this per smoothing rule: Dirichlet `μ ∈ {100, 2000, 10000}` and
   Jelinek–Mercer `λ ∈ {0.2, 0.5}`.
10. **Smoke gate:** run all queries through both engines once; if either throws or drops queries, fix
    the grammar (both must accept 100% of generated queries) before diffing.

### C. Differential harness  → `scripts/trec-comparison/diff_fuzz.sh` (+ Python diff/minimizer)

11. Run both engines (`count = 1000`, `trecFormat`). For each query, join by `(qid, docno)` and compare
    **scores on the shared docs** with tolerance `1e-3` (scores align to ~1e-5 on identical integers, so
    1e-3 is safe); also record set overlap@{10,100,1000}. Compare **scores, not ranks** (equal-score
    tie-breaking differs harmlessly at the tail).
12. Classify each query MATCH / DIVERGE; for DIVERGE, record a signature (sorted set of operators used
    + max |Δ| + whether set or score差). Summary: queries run, exact-match %, ranked signatures.
13. **Auto-minimize** each diverging query (delta-debug): repeatedly try (a) replacing a subtree with
    one of its leaves, (b) dropping an operand, (c) shrinking `N`/weights, re-running both engines, and
    keep the reduction if it still diverges. Also restrict to the few docs that carry the delta. Emit
    the minimal repro per signature.

### D. Triage, fix, report

14. For each distinct minimal repro: hand-compute the expected value on that tiny case (do **not**
    assume Indri is right — cross-check with the Dirichlet/belief formulas and ordered-vs-unordered
    consistency, per TASK-0010). Then either **fix** (with a count/score-level regression test, reusing
    the single-doc `tf = round(L·e^score)` trick) or **catalog** with rationale.
15. Re-run the whole fuzz suite after each fix; converge to a high exact-match rate. Append findings to
    `docs/lucindri-vs-indri-scores.md`.

### Artifacts to commit
- `scripts/trec-comparison/build_integer_corpus.sh` (+ Python helper; `dump_forward` for full scale).
- `scripts/trec-comparison/fuzz_queries.py`, `diff_fuzz.sh` (+ minimizer).
- `vocab.tsv` builder is committed; `integers.trec`, the indexes, and raw runs stay under `$WORK`
  (gitignored / external), per the repo convention. Fixed `--seed` recorded so runs are reproducible.

### Gotchas (learned this session)
- `dv` per-doc is slow at scale → Indri-API dump for full LATimes.
- Compare scores, not ranks (tie-breaking at the count cutoff is not a real divergence).
- Keep the fuzzer legal (proximity operands must be positional) — the smoke gate enforces it.
- Filter operator names differ (`#filreq`↔`#scoreif`); OOV ids exercise the TASK-0010 floor/proximity
  fixes; include them deliberately.

## Progress log

- 2026-07-04 — Task created (follows TASK-0010, which is Done). Not yet started.
- 2026-07-05 — Added the detailed ordered implementation plan (build via Indri forward index with a
  global string→id map; seeded legal-query fuzzer in both dialects; diff + delta-debug minimizer;
  triage against hand-computed truth). Ready to implement.
- 2026-07-05 — **Implemented & Done.** Built a 20k-LATimes integer collection via `dumpindex il` (one
  pass; global `term→id` by cf); indexed in both engines; Phase-0 aligned exactly (`|C|`≈10M, `cf`,
  positions). Fuzzed 1,500 legal queries (both dialects) and diffed scores. **Found + fixed 3 real
  bugs** (each with a regression test; suite green): (1) cf=0 proximity window dropped not floored
  (`IndriTermOpWeight`); (2) crash on a proximity as a filter condition (`IndriTermQuery` — always
  build `TermStates` with stats); (3) `#syn` with an OOV operand returned empty (operator-specific
  `oovOperandForcesEmpty()` hook). No further structural bug: the residual is the Phase-5
  filter-in-belief renormalization (cataloged; 98% of Δ>1, unusual construction) and the norm
  quantization (**TASK-0012**). Artifacts committed (`build_integer_corpus.sh`/`build_integers.py`,
  `fuzz_queries.py`, `diff_fuzz.py`). Findings in `docs/lucindri-vs-indri-scores.md`. Could scale to
  full LATimes with an Indri-API forward dump and more seeds/queries as future work.
- 2026-07-05 — **Scaled to full LATimes** (same `dumpindex il` technique, no C++ API program needed):
  131,896 docs, |C|≈66.2M, Phase-0 aligned; fuzzed 8,000 queries (seeds 7 + 99), 0 exceptions/crashes.
  This exposed a **4th real bug** invisible on the single-segment 20k build: **term-op collection
  frequency was segment-local**. On a 6-segment index a proximity/synonym op absent from a doc smoothed
  with only that segment's `cf` (often 0 → floored) instead of the collection-wide `cf` Indri uses.
  Matching scores were unaffected (`tf` dominates) so it hid until an absent term-op's background was
  checked at scale — e.g. q1745 `#weight(0.79 #syn(34533 28149) 0.48 #syn(442 517) 0.58 #1(387 1767))`
  on d130788 diverged Δ=2.05, now Δ=0.007. Fixed in `IndriTermOpWeight` (`ensureCollectionStats`
  computes `cf`/`df` across all leaves once; absent-in-leaf term-ops return an empty-positions scorer
  that still smooths with the collection-wide background). Regression test
  `proximityBackgroundUsesCollectionWideCfAcrossSegments` (two-part MultiReader) fails before / passes
  after; searcher suite green (49 tests). After this fix the only remaining divergences at full scale
  are the cataloged filter-in-belief renormalization and norm quantization (TASK-0012).
