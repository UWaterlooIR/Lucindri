# TASK-0018 — Fix `#band` nested inside `#band` (scores diverge from C++ Indri)

- **Status:** Ready
- **Created:** 2026-07-05
- **Owner:** (unassigned)
- **Summary:** A `#band` used as an **operand of another `#band`** scores differently from C++ Indri
  (per-doc Δ up to ~1.1 log, ranking-changing). Flat `#band(a b c)`, a lone `#band`, `#band` inside a
  window (`#uwN`/`#combine`), and a window inside `#band` all **match** Indri — the divergence is narrow
  and specific to a `#band` operand of a `#band`. Found while verifying TASK-0012 (exact document length):
  once norm quantization was removed, this surfaced as a distinct, **length-independent** structural
  residual, separate from the cataloged filter-in-belief renormalization (TASK-0011 §5).

## Evidence (full-LATimes integer collection, μ=2000, exact-length Lucindri index)

Reproduced with `scripts/trec-comparison/band_probe.sh` (built-in isolation battery). Same query text runs
in both engines (integers ⇒ analysis is a no-op). Top-of-ranking, Indri vs Lucindri:

| query | verdict |
|---|---|
| `#band( 492 732 577 )` (flat) | **match** (~1e-6) |
| `#band( 732 577 )` (inner alone) | **match** (~1e-6) |
| `#band( 492 #uw8( 732 577 ) )` (window in band) | **match** (~1e-6) |
| `#uw8( 492 #band( 732 577 ) )` (band in window) | **match** (~1e-5) |
| `#combine( 492 #band( 732 577 ) )` (band in belief) | **match** (~1e-6) |
| **`#band( 492 #band( 732 577 ) )`** (band in band) | **DIVERGES** — Indri top docs d27293 (−7.197), d282 (−7.336) are **absent** from Lucindri's ranking; Lucindri tops d99770 (−7.066), whose Indri score is −7.757 |

From the TASK-0011 fuzz re-run (1,500 queries), the only non-filter queries with maxΔ > 0.05 after exact
length were exactly these — all `#band`-in-`#band` (or a window over a `#band`):
- `q625: #band( 355 #band( 620 395 ) )`  Δ≈0.68
- `q776: #od2( #band( 330 472 ) 1 )`     Δ≈0.69
- `q967: #band( 492 #band( 732 577 ) )`  Δ≈1.10

Their Δ is ~identical with `exactDocumentLength` on or off (0.68 vs 0.68, 1.10 vs 1.08), confirming this is
**not** a document-length effect.

## Suspected cause

`#band` has its own implementation (`IndriBandQuery` / `IndriBandWeight` / `IndriBandScorer`), separate from
the window operators (`IndriTermOpWeight` + `IndriUnorderedWindowWeight` / `IndriOrderedWindowWeight`).
Because `#band`-in-`#uw` (outer = window) works but `#band`-in-`#band` (outer = band) fails, the suspect is
the **outer band's** occurrence-building:
`IndriBandWeight.createInvertedList` (`LucindriSearcher/.../IndriBandWeight.java`, the position sweep at
lines ~56–99). It reads each operand's `nextPosition()/endPosition()` and runs a `termsFound`/
`previousTermPosition` window sweep that assumes term-like single positions. When an operand is itself a
`#band` (an `IndriTermOpEnum` emitting wide `(start,end)` extents, possibly overlapping), that sweep appears
to count/emit extents differently from Indri's `BAndNode` extent-AND. The mismatch only manifests when both
the outer and inner op are `#band` (the inner `#band`'s extents fed into this exact sweep).

Note C++ Indri semantics to check against: `#band` (`indri/BAndNode`) is a pure boolean-AND over the
document — the belief is combined from the operands' beliefs when all are present — it does **not** impose a
proximity window. Confirm what extent list Indri's `BAndNode` exposes to a parent `BAndNode`, and match it.

## Reproduce

- `scripts/trec-comparison/band_probe.sh` — no args runs the isolation battery; one arg probes a single
  query in both engines side-by-side (with divergence flags). Defaults point at the persisted indexes below.
- Persisted test infrastructure (do **not** delete — needed to investigate; rebuild via
  `build_integer_corpus.sh` if lost):
  - **Indri** integer index: `/ssd-8TB/trec-compare/fuzzfull/i` (full-LATimes, 131,896 docs, Phase-0
    aligned; corpus `/ssd-8TB/trec-compare/fuzzfull/isrc/integers.trec`).
  - **Lucindri** exact-length index: `/ssd-8TB/trec-compare/el0012/lexact` (and norm `.../lnorm`), built
    from the same corpus.
- Full differential: `fuzz_queries.py <seed> <M> 800 qi.frag ql.frag`, wrap into Indri `.param` /
  Lucindri `.xml` (see the TASK-0012 log / `el0012/fi.param`,`fl_exact.xml`), run both, `diff_fuzz.py`.
  Filter the residual to `#band`-containing queries to target this bug.

## Acceptance criteria

- A regression test (searcher module, JUnit + `TestIndex`) with a small controlled index where
  `#band( a #band( b c ) )` produces the **same** occurrences/score as C++ Indri — failing before the fix,
  passing after. Include the plain `#band(a b c)` and `#band`-in-`#uw` cases as guards (they must stay
  matching). Choose lowercase, non-stopword, stem-invariant integer/word tokens with known positions.
- The `band_probe.sh` `band-in-band` cases match Indri to ≤ ~1e-4 (rankings agree), and the TASK-0011 fuzz
  (`#band`-containing queries) no longer shows the non-filter structural divergence.
- If C++ Indri's `#band`-in-`#band` semantics turn out to be a genuinely different (intended) model that is
  impractical to replicate on stock Lucene, catalog it in `docs/lucindri-vs-indri-scores.md` and
  `docs/indri-query-language.md` §6 instead, with the measured effect and why.

## Progress log

- 2026-07-05 — Filed from the TASK-0012 verification. Isolated the divergence to `#band` nested directly in
  `#band` (all other nestings match); prime suspect `IndriBandWeight.createInvertedList`. Added
  `scripts/trec-comparison/band_probe.sh` and preserved the integer indexes (`fuzzfull/i`,
  `el0012/lexact`,`lnorm`) as the investigation harness.
