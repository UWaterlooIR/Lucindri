# TASK-0008 — End-to-end Lucindri vs Indri comparison on TREC t45minusCR + topics 401–450

- **Status:** Done (core comparison) — optional tokenizer-isolation follow-up proposed
- **Created:** 2026-07-04
- **Owner:** Mark D. Smucker
- **Summary:** Index a real TREC collection (`t45minusCR`) in **both** C++ Indri and Lucindri, run
  the 50 Indri-language topics 401–450 (Metzler) plus generated queries covering the operators the
  50 don't exercise, and **compare the two systems' rankings** — a scaled stress test of all the
  Lucindri fixes (TASK-0002/0003/0006/0007) that also confirms Lucindri matches Indri reasonably
  well on real data. Investigation/measurement task; findings → the conformance catalog.

> Read `../CLAUDE.md` first. Reference: [[indri-cpp-reference]] (`/ssd-8TB/installs/indri-5.21/`).
> Builds on the small-corpus harness `scripts/diff_indri.sh` (TASK-0005).

---

## 1. Inputs

- **Corpus:** `/ssd-8TB/corpora/t45minusCR/` — 7 **gzipped** trectext files (`ft91–94`, `fr94`,
  `latimes`, `fbis5`); TREC disks 4 & 5 minus the Congressional Record (~500k docs). Tags observed:
  `<DOC>`, `<DOCNO>`, `<TEXT>`, `<HEADLINE>`, `<DATE>`, `<PAGE>`, `<PROFILE>`, `<PUB>`.
- **Queries:** `/ssd-8TB/trec-topics/401-450.metzler.xml` — 50 topics in **Indri query language**
  (TREC-8 ad-hoc). Every topic is `#weight( 0.8 #combine(terms) 0.1 #combine(#1 phrases) 0.1
  #combine(#uwN windows) )` (single-term topics omit the phrase/window parts).

## 2. Operator coverage (verified) and what to generate

The 50 topics use only: **`#weight` (all 50), `#combine`, `#1`, `#uwN` (`#uw8`,`#uw12`)**, plus
plain and numeric terms. So this collection stress-tests those four operators heavily but leaves
most of Lucindri's claimed language untested.

**Generate additional queries** (same collection) to cover the rest and stress the recent fixes:
- `#or`, `#not` (e.g. `#combine(topic #not(unwanted))` — TASK-0006), `#max`, `#wsum`, `#wand`.
- `#scoreif` / `#scoreifnot` (TASK-0006) — `#scoreif(requiredterm #combine(topic))`,
  `#scoreifnot(excludeterm #combine(topic))`.
- `#band`, `#syn`, and **compound operands** — `#band(#syn(a b) #syn(c d))`, `#uwN(#syn(a b) c)`,
  `#1(#syn(a b) c)` (the exact shapes fixed in TASK-0002; include a phrase at document position 0).
- **Field restriction** `term.field` — requires a fielded index (e.g. index `headline`/`title` as
  named fields via `fieldNames`; TASK-0007); then `term.headline`.
- **Jelinek-Mercer** smoothing — run a subset with Lucindri `<rule>jm:0.4</rule>` vs Indri
  `method:linear,collectionLambda:0.4`.

## 3. Prerequisites / wrinkles (resolve before comparing)

1. **Lucindri trectext reads plain files only** (`FileInputStream`), but the corpus is `.gz`. Indri
   `IndriBuildIndex` reads gzip directly. Options: (a) **add gzip support to
   `TrecTextDocumentParser`** — mirror the climbmix parser's `GZIPInputStream` (small, generally
   useful; recommended, possibly its own tiny task); or (b) decompress the corpus for Lucindri (a
   few GB). Do (a).
2. **Content tags:** configure Lucindri `contentTags=text,headline` (TASK-0007) to match Indri's
   include tags (`text`, `headline`, `title`, …) so both index the same body. Confirm Indri's
   trectext include-tag set and align.
3. **Analysis alignment (decided):**
   - **Stopwords — align Indri to Lucindri's set.** Lucindri stops with Lucene's `EnglishAnalyzer`
     default set at index time (`removeStopwords=true`) AND query time (hardcoded on — not
     configurable). That set is exactly these **33 words** (extracted from the jar):
     `a an and are as at be but by for if in into is it no not of on or such that the their then
     there these they this to was will with`.
     Indri does no stopping by default, so give Indri the **same 33 words** as a `<stopper>` in the
     `IndriBuildIndex` parameter file (Indri then stops both the index and queries with it).
     Generate the `<stopper>` block mechanically (don't hand-type):
     `for w in a an and are ... with; do printf '<word>%s</word>' "$w"; done`.
     Keep `removeStopwords=true` on the Lucindri index so `|C|` excludes the same words on both
     sides. (Order note: Lucindri stops *before* stemming; all 33 are stem-invariant, so order is
     moot.)
   - **Smoothing — set explicitly, don't trust defaults.** Dirichlet: Lucindri `dirichlet:2000` =
     Indri `method:dirichlet,mu:2000`. JM: Lucindri `jm:0.4` = Indri
     `method:linear,collectionLambda:0.4`. (Lucindri's default μ=2000 differs from Indri's default,
     so an *unset* rule silently diverges — always set it.)
   - **Stemming + tokenizer — ACCEPT AND CATALOG (per owner).** Run each engine's native analyzer
     (Lucindri **KStem** / Indri **Krovetz**, same family; StandardTokenizer vs Indri's word
     tokenizer). The residual differences in which tokens exist — and hence `p(w|C)` — are the
     TASK-0005 **Category-B** differences; record them, do **not** treat them as bugs. (Optional
     isolation run: stemming off in both, to quantify how much of any divergence is stemming; the
     headline comparison still uses the real analyzers.)

## 4. The central hypothesis to test: `#weight`

Every topic is wrapped in `#weight`. In Lucindri the parser recognizes `weight` and parses the
per-clause weights, but `createBooleanClause` has **no `#weight` branch** → it builds an
`IndriAndQuery` with the weights applied as `BoostQuery` boosts. So Lucindri's `#weight` ≈ a
**boosted `#combine`**, not necessarily Indri's `#weight` (weighted combination of beliefs).
**Determine whether Lucindri's `#weight` matches Indri's** — if not, all 50 topics diverge, and
that's a finding (`#weight` is what real Indri query generators emit, so it matters) → likely a
follow-up fix task.

## 5. Methodology

1. **Index both** (once): Indri via `IndriBuildIndex` (gz direct, Krovetz, stoplist); Lucindri via
   `BuildIndex` (`documentFormat=trectext`, `contentTags=text,headline`, `kstem/true/true`, gz
   support from §3). Confirm comparable document counts.
2. **Run** all 50 topics + the generated queries in both, TREC format, `count=1000`
   (`IndriRunQuery` vs `IndriSearch`); align μ (dirichlet:2000) and the JM subset.
3. **Compare rankings** (per query + aggregate) — this is a *system agreement* study:
   - **Overlap@k** (fraction of shared docs) at k = 10, 100, 1000.
   - **Rank-biased overlap (RBO)** (top-weighted) and/or **Kendall's τ** on the common docs.
   - Summarize: mean overlap@10/@100, and the queries with the largest disagreement.
4. **Attribute divergences** to: (a) `#weight` semantics (§4), (b) KStem vs Krovetz / stopwords,
   (c) tie-breaking among equal scores (benign), or (d) a real bug (→ new task).
5. **Optional effectiveness check:** with the TREC-8 ad-hoc qrels for 401–450, compute MAP / P@10
   for **each** engine and compare — do they achieve *similar effectiveness*? (Agreement is the
   primary goal; this is a sanity check that both are "reasonable.")

## 6. Harness & artifacts — where things live (reproducibility)

Rule: **commit the recipe + inputs + the findings summary; keep large regenerable outputs
(indexes, raw runs) out of the repo.**

- **Harness (committed):** `scripts/compare_trec.sh` — builds both indexes, runs a topics file
  through both engines (`IndriRunQuery` / `IndriSearch`), and computes per-query overlap@k + RBO
  and an aggregate table. Env-configurable: `CORPUS`, `TOPICS`, `INDRI_BIN`, the Lucindri jars,
  `MU`, and `WORK` (output dir). Reuse the build/run/normalize pieces from `scripts/diff_indri.sh`.
- **Committed inputs** (small; the reproducible experiment definition), under
  `scripts/trec-comparison/`:
  - `generated-queries.xml` — the extra Indri-language queries covering the uncovered operators (§2).
  - `english-stopwords.txt` — Lucindri's 33-word set (one per line) + `make-indri-stopper.sh` to
    emit the Indri `<stopper>` block from it.
  - `indri-build.param.tmpl` and `lucindri.properties.tmpl` — the two index-build configs (paths as
    `${VARS}`), so the exact indexing settings are recorded.
- **NOT committed** (large / regenerable): the corpus (`/ssd-8TB/corpora/t45minusCR`, external), the
  two indexes, and the raw per-query TREC runs + overlap CSVs → written to `$WORK` (default outside
  the repo, e.g. under `/ssd-8TB/...`). If a local output dir is placed in-repo, gitignore it
  (`scripts/trec-comparison/out/`). The 401–450 topics stay at their external path
  (`/ssd-8TB/trec-topics/401-450.metzler.xml`); the harness references it by env/path, never copies it in.
- **Findings (committed):** `docs/trec-comparison.md` — methodology recap, the alignment-verification
  results (§5), the agreement tables (mean overlap@{10,100,1000}, RBO; biggest-disagreement queries),
  the `#weight` verdict, and the divergence triage. Link it from `docs/README.md`. It is the
  large-scale complement to the 6-doc catalog `docs/lucindri-vs-indri.md`.

## Implementation plan (ordered)

1. **Branch** off `master` (e.g. `feature/trec-comparison`).
2. **Add gzip support to `TrecTextDocumentParser`** (prereq — §3.1): wrap the `FileInputStream` in
   `GZIPInputStream` when the file name ends in `.gz` (mirror the climbmix parser); add a unit test;
   rebuild the indexer jar. (Small; can be its own commit.)
3. **Build the Indri index** from the gz corpus: `IndriBuildIndex` param with the 7 gz files,
   `<stemmer><name>krovetz</name></stemmer>`, the 33-word `<stopper>` (§3.3), and confirm the
   trectext include-tag set. Record the doc count.
4. **Build the Lucindri index** (same corpus): `documentFormat=trectext`, `dataDirectory` = the gz
   corpus, `contentTags=text,headline` (align to Indri's include tags), `stemmer=kstem`,
   `removeStopwords=true`, `ignoreCase=true`, `indexFullText=true`. Record the doc count.
5. **Verify alignment BEFORE comparing rankings** (cheap, catches misconfiguration):
   a. Doc counts comparable across engines.
   b. Collection stats: Indri `dumpindex <idx> s` (total terms = `|C|`) vs Lucindri
      `collectionStatistics("fulltext")`; `|C|` and a few `cf(w)` should be close (stemmer/tokenizer
      residual aside). A large gap ⇒ stopwords/`contentTags` misaligned → fix before proceeding.
   c. One single-term query in both — top scores should match to a few decimals (the TASK-0005
      trick). If not, μ / stopwords / collection model is off.
6. **Run the 50 topics** in both, TREC format, `count=1000`, `dirichlet:2000` /
   `method:dirichlet,mu:2000`.
7. **Generate + run the extra queries** (§2) — `#or`/`#not`/`#max`/`#wsum`/`#wand`/`#scoreif`/
   `#scoreifnot`/`#band`/`#syn`/compound-operand/`term.field`/JM — over the same collection.
8. **Compute agreement** (§5): per-query overlap@{10,100,1000} + RBO/τ; aggregate means; list the
   biggest-disagreement queries.
9. **Answer the `#weight` question** (§4): do the 50 topics agree with Indri? A systematic
   divergence is the headline finding → file a `#weight` fix task.
10. **Triage divergences** into: `#weight` semantics / KStem-vs-Krovetz + tokenizer (accept &
    catalog) / tie-breaking (benign) / real bug (→ new task).
11. **Optional effectiveness** with the TREC-8 ad-hoc qrels (401–450): MAP / P@10 per engine,
    compared (sanity that both are "reasonable," not a bake-off).
12. **Record & commit per §6:** harness → `scripts/compare_trec.sh`; inputs →
    `scripts/trec-comparison/` (generated queries, stopword list, build templates); findings →
    `docs/trec-comparison.md` (linked from `docs/README.md`). Keep indexes/raw runs out of the repo
    (`$WORK`, or gitignore `scripts/trec-comparison/out/`). Close out.

## 7. Acceptance criteria

1. Both engines index `t45minusCR` (comparable doc counts); Lucindri reads the gz trectext (§3).
2. All 50 topics + the generated queries run in both engines and produce TREC output.
3. An **agreement report**: per-query and mean overlap@{10,100,1000} (+ RBO/τ), with the
   `#weight` parity question answered explicitly.
4. Divergences triaged (§4); any real bug filed as a new task; expected differences (stemming,
   tie-breaking) noted.
5. Findings recorded (extend `docs/lucindri-vs-indri.md`, or a new `docs/` note) — this is the
   large-scale complement to the 6-doc catalog.

## 8. Out of scope

- Improving retrieval effectiveness (this is a *behavioral agreement* study, not a bake-off).
- Operators Lucindri does not claim (wildcards, `#prior`, passage/extent retrieval, `#wsyn`, …).
- Fixing `#weight` (if it diverges) — file a follow-up; here we only measure and diagnose.

## 9. Validates (regression at scale)

TASK-0002 (`#band`/`#uwN`/`#1`/`#syn`, compound operands, position-0), TASK-0003 (Dirichlet
scoring parity), TASK-0006 (`#scoreif`/`#scoreifnot`/`#not`), TASK-0007 (trectext + `contentTags`)
— all exercised on a real TREC collection with real Indri-language queries.

## Progress log

- 2026-07-04 — Task drafted (Status: Ready). Verified inputs: 50 topics use only
  `#weight`/`#combine`/`#1`/`#uwN`; corpus is gzipped trectext (TEXT/HEADLINE/…). Two wrinkles to
  resolve first: Lucindri trectext needs gzip support, and `#weight` parity is the central question
  (Lucindri collapses `#weight` to a boosted `IndriAndQuery`). Generated-query set to cover the
  uncovered operators (esp. the TASK-0002/0006 fixes).
- 2026-07-04 — Pinned the analysis-alignment methodology (per owner) and added an ordered
  implementation plan. Stopwords: align Indri to Lucindri's exact 33-word Lucene `EnglishAnalyzer`
  set via `<stopper>` (Lucindri's query-side stopping is hardcoded on; keep `removeStopwords=true`).
  Smoothing: set μ explicitly in both (defaults differ). Stemmer (KStem/Krovetz) + tokenizer
  residuals: **accept and catalog** (Category B), not bugs. Added pre-comparison alignment checks
  (doc counts, `|C|`/`cf` via `dumpindex s` vs `collectionStatistics`, single-term score match).
- 2026-07-04 — **Executed.** Added gzip support to `TrecTextDocumentParser` (+ test). Built both
  indexes (528,155 docs each). Ran all 50 topics. Results in `docs/trec-comparison.md`; harness
  `scripts/compare_trec.sh`, inputs under `scripts/trec-comparison/`. Findings:
  - Full-topic agreement Indri↔Lucindri: overlap@10 **0.428**, @1000 0.411, RBO 0.436, top-1 19/50.
    Term-only (proximity stripped): overlap@10 **0.716**, @1000 **0.854**, top-1 33/50.
  - **`#weight` parity CONFIRMED CORRECT** — parse structure, `IndriAndScorer` boost-weighted
    average, and sequential extreme-weight tests all check out. (The original hypothesis that
    `#weight` collapses to an unweighted `IndriAndQuery` is **false**: per-component weights become
    `BoostQuery` boosts and are applied.) An earlier "weights dropped" result was a **test-harness
    race** — `diff <(runA) <(runB)` had both subshells writing the same temp query file; corrected.
  - **Root cause of divergence = the index tokenizer** (Lucene `StandardTokenizer` vs Indri `word`:
    splits `3.14`→`3`,`14`, strips apostrophes/acronym dots). Verified by dumping indexed terms of a
    synthetic doc. Inflates Indri doc lengths (~55% on number-heavy docs), shifts positions →
    proximity window cf/matches, and is amplified in belief combination via large document-length-
    sensitive proximity backgrounds. Accepted & catalogued per owner.
  - Individual operators match well in isolation (`#combine` 0.90, `#1` 1.00, `#uw8` 0.80).
- 2026-07-04 — Follow-up proposed (owner asked how to isolate the tokenizer out): pre-tokenize the
  corpus once with a single canonical pipeline and feed **both** engines the identical whitespace-
  delimited token stream with each engine's own tokenizer/stemmer/stopper disabled, so positions,
  doc lengths, and collection statistics become byte-identical and any residual is pure query
  semantics. Details in the response; not yet implemented.
