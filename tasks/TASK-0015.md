# TASK-0015 — Negative/robustness fuzzer: Lucindri must reject invalid queries cleanly (never crash, never silently misparse)

- **Status:** Ready
- **Created:** 2026-07-05
- **Owner:** (unassigned)
- **Summary:** We have a *legal*-query differential fuzzer (TASK-0011). We do **not** have a
  *negative* fuzzer that feeds **invalid** queries and checks that Lucindri rejects them gracefully.
  Preliminary probing shows the parser currently **leaks low-level crashes** (`ArrayIndexOutOfBounds`,
  `StringIndexOutOfBounds`, `NumberFormatException`) on malformed input instead of a clean, documented
  error. Build a grammar-aware invalid-query fuzzer, define and enforce a robustness invariant, and fix
  the parser so every bad query is rejected with a clear `IllegalArgumentException` (or handled
  sensibly) — never an unexpected exception, hang, or silent misparse.

## The robustness invariant (the oracle)

For **any** input string `s`, `new IndriQueryParser().parseQuery(s)` (and running the result on a small
index) must satisfy:

1. **Only `IllegalArgumentException` escapes.** It either returns a usable `Query`/result, or throws
   `IllegalArgumentException` with a clear, actionable message. It must **never** throw any other
   `Throwable` — no `NullPointerException`, `ArrayIndexOutOfBoundsException`,
   `StringIndexOutOfBoundsException`, `NumberFormatException`, `StackOverflowError`, `OutOfMemoryError`.
2. **It terminates** — no infinite loop or pathological blow-up (guard with a timeout and a nesting/size
   cap).
3. **No silent misparse of a structurally invalid query** — a query that violates the grammar
   (unbalanced parens, a belief operator used as a proximity operand, a weighted operator with a
   dangling weight, an unknown operator) must be rejected, not quietly reinterpreted as something else.

This invariant is the fuzzer's checkable property; it needs no second engine. (Optionally, cross-check
against C++ Indri: where Indri rejects and Lucindri accepts, or vice versa, flag for review.)

## Evidence — confirmed parser crashes (probed 2026-07-05 on `lucindri_idx`)

| invalid query | current behavior | should be |
|---|---|---|
| `#uw( alpha beta )` (window, no N) | **ArrayIndexOutOfBoundsException** | `IllegalArgumentException` "missing window distance" |
| `#combine( alpha beta` (missing `)`) | **StringIndexOutOfBoundsException** | `IllegalArgumentException` "unbalanced parentheses" |
| `#combine alpha beta )` (missing `(`) | **StringIndexOutOfBoundsException** | `IllegalArgumentException` "unbalanced/misplaced parentheses" |
| `#weight( abc alpha )` (non-numeric weight) | **NumberFormatException** | `IllegalArgumentException` "invalid weight 'abc'" |
| `#9999999999( alpha beta )` (distance overflows int) | **NumberFormatException** | `IllegalArgumentException` "invalid window distance" |
| `#combine( )` (empty operator) | no error, 0 results | decide: reject, or define as empty (documented) |

Cleanly handled already (regression targets to keep green): belief operator inside a proximity operator
(`#1(#combine(peace talks) summit)` → `IllegalArgumentException`), and unknown/unimplemented operators
(`#foo(...)`, `#wsyn(...)` → `IllegalArgumentException`, per TASK-0014).

## Known parser weak points (grounded in `IndriQueryParser.java`)

- **The parenthesis-balance check is commented out.** In `parseQueryString`, the code computes
  "missing/unbalanced/misplaced parentheses" but the `throw` is commented out, so malformed-paren
  queries fall through to `substring(...)`/`lastIndexOf(")")` and crash with
  `StringIndexOutOfBoundsException`. Re-enable it as an `IllegalArgumentException`.
- **`createOperator` proximity parsing is unguarded:** `#uw`/`#near`/`#window` with no number →
  `split(...)[1]` `ArrayIndexOutOfBounds`; `Integer.parseInt(distance)` → `NumberFormatException` on a
  huge/negative/non-numeric N. Validate the distance (positive int in range) and error clearly.
- **`popWeight` uses `Float.valueOf(...)`** on the weight token → `NumberFormatException` for a
  non-numeric weight, and a weighted operator (`#weight`/`#wand`/`#wsum`) with an odd token count leaves
  a dangling weight. Validate.
- **Deep nesting** (`#combine(#combine(#combine(…)))`) risks `StackOverflowError` in the recursive
  descent — cap depth and reject beyond it.

## Plan

1. **Invalid-query generator** (`scripts/trec-comparison/fuzz_invalid.py` or a new `scripts/fuzz/`).
   Seeded and reproducible. Combine three strategies:
   - **Grammar-aware invalids:** deliberately violate one rule per query — unbalanced/misplaced/extra
     parens; operator with no operands (`#uw2()`, `#syn()`); missing window distance (`#uw()`, `#near()`);
     out-of-range distance (`#0(a b)`, `#99999999999(a b)`, `#-1(a b)`); malformed weights
     (`#weight(a b)`, `#weight(0.5 a b)`, `#weight(abc a)`); belief op as a proximity operand
     (`#1(#or(a b) c)`); unknown operators and typos (`#combyne`, `#wsyn`, `#prior`); trailing/leading
     field dots (`a.`, `.title`); empty text.
   - **Mutation of legal queries:** take queries from the TASK-0011 legal fuzzer and corrupt them
     (delete/insert/swap a char, drop a paren, blank a weight, splice a belief op into a proximity slot).
   - **Adversarial/degenerate:** very deep nesting, thousands of operands, huge tokens, control/Unicode
     characters, only-whitespace, only-punctuation.
2. **Harness that enforces the invariant.** A small Java driver (reuse the `TestIndex` fixture or a tiny
   fixed index) that reads one query per line, calls `parseQuery` + a bounded search, and classifies the
   outcome as OK / `IllegalArgumentException` / **VIOLATION** (any other throwable, a timeout, or a
   suspicious silent-accept). Print/collect every violation with the offending input.
3. **Fix the parser** to uphold the invariant: re-enable paren validation; guard proximity distance
   parsing (missing/over-range/non-numeric → `IllegalArgumentException`); validate weights in `popWeight`
   and require matched weight/operand counts; cap recursion depth; make sure every internal failure is
   surfaced as `IllegalArgumentException` with a message that names the problem. Do **not** change any
   behavior for *legal* queries (re-run the TASK-0011 legal fuzz to prove no regression).
4. **Lock it with a JUnit regression test** (`InvalidQueryRejectionTest` in `LucindriSearcher`): a
   curated, seeded corpus of invalid queries (at least one per evidence row above plus a sample of each
   category) asserting the invariant — each currently-crashing case throws `IllegalArgumentException`
   (fail-before/pass-after), and the belief-in-proximity / unknown-operator cases stay rejected. Keep the
   large random generator as a script for periodic runs.

## Acceptance criteria

- A committed, reproducible negative fuzzer + harness under `scripts/`.
- Running it over a large seeded corpus produces **zero invariant violations** (no non-IAE throwable, no
  hang, no silent misparse) after the fixes.
- A green JUnit `InvalidQueryRejectionTest` that fails on today's parser (the crashes above) and passes
  after; the legal-query behavior and the full searcher suite remain green.
- The confirmed crash rows above all become clean `IllegalArgumentException`s with clear messages.
- Decision recorded for the genuinely ambiguous cases (e.g. `#combine()` empty operator): reject, or
  define-and-document.

## Notes / scope

- Scope is the searcher query parser + its scorer entry points (`IndriQueryParser`, and any crash that
  only appears at search time). Indexer/analyzer untouched.
- Rationale (owner, 2026-07-05): a search engine must not crash or silently misinterpret a user's
  malformed query — it should reject it with a clear message. This complements TASK-0014 (which stopped
  *unknown operators* from silently degrading); this task hardens the parser against *all* malformed
  input.
- Related: TASK-0014 (reject unknown operators), TASK-0011 (legal-query differential fuzzer whose corpus
  feeds the mutation strategy).

## Progress log

- 2026-07-05 — Task created. Probing confirmed the parser leaks `ArrayIndexOutOfBounds`,
  `StringIndexOutOfBounds`, and `NumberFormatException` on malformed input (table above), and that the
  parenthesis-balance check in `parseQueryString` is present but commented out. Belief-in-proximity and
  unknown-operator rejection already work (clean `IllegalArgumentException`). Ready to implement.
