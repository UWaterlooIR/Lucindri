# TASK-0022 — Refine query-biased summaries (bound their size)

- **Status:** Ready
- **Created:** 2026-07-08
- **Owner:** (unassigned)
- **Summary:** The opt-in `/search` summaries (TASK-0019 §4) are correct but **unbounded in size** — a
  document with few sentence boundaries yields one enormous passage. **Decision:** keep the current
  sentence-passage `UnifiedHighlighter` approach and add a **hard word cap** on the result (default 75
  whitespace-separated words), plus make both the passage count and the cap **server-configurable**.
  Selected sentences are joined by a **single space** (no inter-sentence ellipsis); an ellipsis (`...`) is
  appended **only** to mark end-of-summary truncation. No ranking change.

## Motivation / observation

Summaries are produced by `UnifiedHighlighter` with a sentence `BreakIterator`, joined by `" ... "`
(`LucindriSearchService.summarize()`). Passage boundaries are whatever the sentence `BreakIterator`
finds — so when a document has a "sentence" that runs for a full paragraph (dense regulatory / legal
text, or an SEO keyword wall), a single passage can be enormous.

Observed live on the t45mCR index (2026-07-08), query `#combine(#1("black bear") "attacks")`, result
**`FR940902-1-00053`** (a Federal Register hunting regulation): the summary was a single ~2,000-character
"sentence" of sub-clauses `(B) … (viii) … (ix) …`. Also seen on climbmix: SEO product-keyword pages whose
entire body is one punctuation-free run-on. Not a highlighter bug — the source genuinely has no sentence
break — but a bad snippet.

**Why it matters:** the ISJ/Cottontail consumer feeds these summaries to an LLM judge. Unbounded snippets
waste tokens and can dominate the prompt; a bounded, predictable summary size is wanted. This is UX/cost
polish, not a correctness issue.

## Options considered (design discussion 2026-07-08) — and why A

- **A (chosen) — sentence passages + hard word cap.** Keep the sentence `BreakIterator` and
  UnifiedHighlighter's query-biased passage selection; after it returns the joined passages, truncate to a
  **word cap** (word boundary + marker). Simplest; reuses UnifiedHighlighter's stemmed-term location and
  BM25-ish passage scoring; clean on well-punctuated docs.
- **B — word/line base iterator + length goal.** Robust to run-ons (can select a window anywhere), but
  breaks *clean* text mid-sentence and adds no value over A's cap for the judge. Rejected.
- **C — custom "bounded-sentence" `BreakIterator`.** Best quality (sentence boundaries for clean text, forced
  word-boundary breaks inside over-long sentences), but ~50–80 lines of custom iterator. Overkill for v1.
- **D — roll our own sliding/overlapping window summarizer.** Best query-centering (half-overlap or
  match-centered windows, word-accurate length), but the most code and re-implements the stemmed-term
  offset location UnifiedHighlighter already does. Revisit only if A's snippets prove inadequate.

**Findings that pinned the decision:**
- `LengthGoalBreakIterator` only *chooses among the base iterator's boundaries* — it never splits within a
  segment, so wrapping the sentence iterator does **not** cap a punctuation-free run-on (my original draft
  was wrong on this). Capping the messy pages therefore has to be a post-step (A) or a different base
  iterator (B/C) or a custom window (D).
- UnifiedHighlighter passages are **contiguous and non-overlapping**, so it can't re-center a window on a
  match near a boundary. Better centering is only possible with D. Accepted for v1.
- Summarization cost is a **per-document re-analysis run only on the top-K results being summarized (≈5)**,
  not the result set or corpus — so it's cheap regardless of A/B/C, and cost is not a deciding factor.
- **Accepted limitation:** on a run-on, A's cap keeps the document's *first* 75 words, which may miss a
  query match deep in the run-on. Acceptable — 75 words of an SEO/keyword wall is low-value to the judge no
  matter how it's sliced.

## Implementation plan (detailed)

### 1. `LucindriSearchService` (`searcher.service`)

**Summary shape — `summarize()`:**
- **Join sentences with a single space, not `" ... "`.** Change the formatter to
  `new DefaultPassageFormatter("", "", " ", false)` (the 3rd arg is the inter-passage separator). There is
  no inter-sentence ellipsis anymore; `...` appears **only** as the truncation marker.
- Keep the sentence `BreakIterator`, the query-term `BooleanQuery`, the `setMaxNoHighlightPassages(maxPassages)`
  leading-sentence fallback, and `setMaxLength(1_000_000)` exactly as-is.
- After `uh.highlight(...)`, cap each returned summary with `capWords(summary, maxSummaryWords)` and return.

**`capWords` (new **package-private static**, so it is unit-tested directly):**
```java
static String capWords(String s, int maxWords) {
    if (s == null) return null;                    // search() maps null -> ""
    Matcher m = Pattern.compile("\\S+").matcher(s);
    int count = 0, endOfCap = -1;
    while (m.find()) {
        count++;
        if (count == maxWords) endOfCap = m.end();
        else if (count > maxWords) return s.substring(0, endOfCap) + " ...";
    }
    return s;                                       // <= maxWords words: unchanged, no marker
}
```
- Counts whitespace-delimited words — **all content**, since the joiner is a space (no `...` tokens to
  exclude).
- `> maxWords` → keep the first `maxWords` words (original text preserved up to the cut) + `" ..."`.
- `== maxWords` → not truncated (boundary). `null` → `null`; `""` → `""`. The `" ..."` marker is appended
  **after** counting, so it never counts toward the cap. Applies to highlighted results and the fallback.

**Constructor plumbing:**
- Add `public static final int DEFAULT_MAX_SUMMARY_WORDS = 75;` and `private final int maxSummaryWords;`.
- New **7-arg** constructor `(index, rule, stemmer, removeStopwords, ignoreCase, maxPassages, maxSummaryWords)`
  holds the body; the existing **6-arg** delegates with `DEFAULT_MAX_SUMMARY_WORDS`, so `IndriSearch` and the
  existing tests need no change.

### 2. `LucindriServer` (CLI)
- New flag `--maxSummaryWords` (default `75`); change the `--maxPassages` default `2` → `4`.
- Validate both parse as integers `>= 1`; otherwise print usage and `System.exit(2)` (fail fast).
- Construct via the 7-arg service constructor; add `maxSummaryWords` to the startup log line and the usage
  text (`[--maxPassages 4] [--maxSummaryWords 75]`).

### 3. `IndriSearch` (batch)
- Its `new LucindriSearchService(..., 2)` uses the 6-arg overload (cap defaults to 75; batch never
  summarizes). Bump the inert hardcoded `2` → `4` for cosmetic consistency (no behavioral effect).

### 4. Tests
- **`capWords` direct unit tests** (new): under-cap (unchanged, no marker); over-cap (exactly N words +
  `" ..."`); exactly-N boundary (not truncated); `null`; `""`; multi-space/newline input; original text
  preserved up to the cut.
- **Service integration** (extend `LucindriSearchServiceTest`): a run-on doc (one long punctuation-free
  line) + a short doc; build the service with a small `maxSummaryWords` (7-arg ctor); `search(...,
  summaries=true)` → run-on summary ≤ N words ending `" ..."`, short-doc summary unchanged. Confirms the cap
  is wired through the highlighter path and that sentences join with a space.
- Existing searcher (96) + server (10) tests stay green.

### 5. Docs
- **README** server section: document `--maxPassages` (default 4) and `--maxSummaryWords` (default 75), the
  word cap, the space-join, and that `...` marks truncation only.
- **`docs/query-biased-summaries.md`** + **TASK-0019 §4**: record the cap, the space-join, and the accepted
  run-on limitation (cap keeps the first N words).

### 6. Verification
- `mvn -o clean install` (reactor green).
- Live re-check on t45mCR: bring the server up at defaults and confirm `FR940902-1-00053`'s black-bear
  summary is now ≤ 75 words ending `" ..."` (the exact case that motivated this).

## Non-goals

- No change to ranking/scoring or to the search contract.
- Not proximity-faithful highlighting, overlapping/match-centered windows, or a custom summarizer (options
  C/D) — deferred; revisit only if A's bounded snippets prove inadequate for the judge.
- Not a per-request knob — cap and passage count are startup config for v1 (consistent with TASK-0019 §5).

## Progress log

- **2026-07-08** — Filed after a live demo on t45mCR: `#combine(#1("black bear") "attacks")` returned a
  ~2,000-char single-sentence summary for a Federal Register doc (`FR940902-1-00053`). Summaries work but
  need a size bound. Relates to TASK-0019 §4.
- **2026-07-08** — Design discussion with owner. Walked options A–D; discovered `LengthGoalBreakIterator`
  can't split a run-on (kills the original draft's plan), that UnifiedHighlighter passages are
  non-overlapping (no match-centering without a custom summarizer), and that summary cost is a cheap
  per-top-K re-analysis. **Decision:** Option A — keep sentence-passage highlighting, add a hard word cap.
  Config: `--maxPassages` default **4** (was 2) and new `--maxSummaryWords` default **75**; truncation
  appends `" ..."` (marker excludes from the count); summaries ≤ cap unchanged. Status → Ready. Awaiting
  review before implementation.
- **2026-07-08** — Wrote the **detailed, file-by-file implementation plan** (the section above) into the
  task per owner request. Owner refinement on joining: **do not** put ellipses between sentences — join
  selected sentences with a **single space**, and use `...` only as the end-of-summary truncation marker.
  This also makes the "joiners don't count toward 75" question moot (there are no joiner tokens; every
  whitespace-delimited token is a content word). Concretely: change the `DefaultPassageFormatter` separator
  from `" ... "` to `" "`; `capWords` counts `\S+` tokens and appends `" ..."` only on truncation. Plan
  committed for review; still not implemented.
